Session 8 — 2 fixes found by cloning the repo into a clean directory and running it with no FRED key. Build both in this session, then write the status file and push. Rule 4 applies.

Per rule 11: first save this message verbatim as instructions/08_fresh_clone_fixes.md and commit it as `08: instructions`.

1. pytest must pass on a fresh clone. On a clean clone, data/processed/ is empty (convention 14), and tests/test_run.py::test_run_is_idempotent_for_a_cheap_section fails with FileNotFoundError on data/processed/rules_labels.parquet. Make the test build what it needs: run the sections its cheap section depends on, in a tmp_path or into the repo's own data/processed, before the idempotence comparison, rather than assuming a populated directory. Do not weaken the comparison, do not skip the test, and do not commit data/processed. Then audit every other test the same way: for each test that reads a path under data/processed or outputs, either it builds what it reads or it is already independent. List the audit result in the status file, test by test, with the paths each one reads.

2. The K = 5 guard catches too narrow an exception. run.py step 6.1 catches np.linalg.LinAlgError. On a different BLAS, hmmlearn's _utils._validate_covars raises ValueError("component 4 of 'full' covars must be symmetric, positive-definite") first, nothing catches it, and python -m regime.run aborts in step 6.1 so sections 6 and 7 never run. Widen the guard to catch both that ValueError and LinAlgError, record the same singular_covariance status and the exception text in k_variant_outcomes.csv either way, and keep every other exception uncaught. Add tests/test_robustness.py::test_singular_variant_is_recorded_not_raised, parametrised over both exception types, asserting the run continues and the outcome row is written. The reviewer's Option A is unchanged: nothing in hmm_numpy.py moves and no pseudo-inverse
