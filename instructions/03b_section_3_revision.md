Section 3 revision — full specification. Build all of it in this session without pausing. Rule 4 applies. This is the specification; nothing below is yours to design.

0. Workflow change, done first and committed on its own as `workflow: instructions and status live in the repo`.
   - Create instructions/. Save this entire message, verbatim, as instructions/03b_section_3_revision.md.
   - Add rule 11 to CLAUDE.md: "Every session begins by saving its instruction message verbatim as instructions/NN_<name>.md (NN = next number, names match the section) and committing it before any other work. Every session ends by writing instructions/NN_<name>.status.md with: outcome (completed / stopped), the step reached, the reason for any stop, every question or blocker for the reviewer, and the final git log origin/main --oneline -3; then committing and pushing it. This applies to sessions that stop early, refuse under rule 1, or hit rule 4: the status file is always written and always pushed. The reviewer reads instructions/, review/ and the repo only; nothing is relayed by chat."
   - Back-fill the history in one file, instructions/00_history.md: the session 0 to section 3 instructions are not available verbatim; list the sessions and their commits, and say so.
   - Session 3 revision number is 03b; the next section uses 04.

Context: the section 3 run shows the anchor feature dgs10_chg12 has no spread across states (state means −0.15 to +0.5), the Hungarian check disagrees at 13 of 21 refits, and 15 of the 26 filtered label changes from 2005 fall exactly on a refit date. Anchoring by sort is replaced by chaining. The primary feature set is then chosen by the rule below, on classifier diagnostics only. No factor return may be read in this session.

1. Anchoring (regime/models/anchor.py, PLAN.md step 3.4, docs/CONVENTIONS_RESOLVED.md item 16).
   - The first refit (first_window_end) is anchored by the existing anchor_permutation rule, unchanged, including its tie handling.
   - Every later refit is anchored by chain_permutation(prev_means, new_means) -> perm: scipy.optimize.linear_sum_assignment on the Euclidean distance matrix between the previous refit's anchored state means and the new state means, over all model-input columns; new state j is the old state matched to previous anchored state j. linear_sum_assignment is deterministic; no further tie-break is applied. Apply perm to startprob, both axes of transmat, means and covars as before.
   - Replace anchor_agreement.csv with anchor_chain.csv: refit_date, state, matched_distance, sort_rule_state (the state the old sort rule would have given it). Report, do not act on, any matched_distance above 1.0 z. hungarian_agreement may be deleted if nothing calls it; chain_permutation replaces it.
   - GMM: chained the same way within the GMM refits; its first refit is chained to the HMM's first anchored refit means.
   - Smoothed HMM: its states are chained to the last expanding refit's anchored means, so smoothed and filtered numbering agree.
   - Tests: tests/test_anchor.py::test_chain_permutation_recovers_a_shuffle (known means, shuffled, perm recovers the order), ::test_chain_permutation_applied_consistently (transmat equals T[perm][:, perm] with the same perm used on means, covars, startprob). Keep the existing anchor_permutation tests; remove test_hungarian_identity_and_swap only if hungarian_agreement is deleted.

2. Primary feature set selection, pre-registered.
   - config.toml: add [features] primary = "core". Every model_input call in sections 3 onward uses the columns named by cfg.features_primary ("core" -> cfg.features_core, the existing d = 8 set; "core_no_level" -> cfg.features_core_no_level, the existing d = 7 set). Update the round-trip test if needed.
   - Run the full section 3 classifier pipeline (expanding HMM, smoothed HMM, expanding GMM, tables) for both column sets with chained anchoring, K = primary_K from the d = 8 BIC (3) for both. Main outputs for "core"; the d = 7 run writes under outputs/regimes/robustness/nolevel/ and outputs/tables/robustness/nolevel/. Also write bic_by_k for d = 7 on the first window to outputs/tables/robustness/nolevel/bic_by_k.csv, information only; K is not reselected.
   - Write outputs/tables/classifier_diagnostics.csv, one row per column set, columns: feature_set, n_filtered_changes (label changes from 2005-01-31 on), n_changes_on_refit_dates (changes where the decision date equals a refit date), share_on_refit_dates, median_run_months, max_expected_duration (largest finite 1/(1−A_kk) over all refits and states), n_infinite_durations, n_degenerate_states (state_counts rows flagged degenerate), max_matched_distance, detects_2008 (filtered hard label at 2008-11-30 differs from 2008-06-30; both under the 2007-12-31 refit), detects_2020 (label at 2020-04-30 differs from 2019-12-31; both under the 2019-12-31 refit), filtered_smoothed_agreement (share of dates from 2005-01-31 where filtered and smoothed hard labels match).
   - Decision rule, applied mechanically: primary becomes "core_no_level" if and only if the d = 7 row has detects_2008 true AND detects_2020 true AND n_changes_on_refit_dates strictly below the d = 8 row's. Otherwise primary stays "core". Write the result into config.toml features.primary and into decisions/primary_feature_set.md, with the diagnostics table pasted in and the rule quoted. If the rule selects "core_no_level", the main outputs (outputs/regimes/*, outputs/tables/*) are regenerated from d = 7 and the d = 8 run moves to robustness/level/.
   - Convention 15 is rewritten as: "The primary feature set is chosen by a pre-registered rule on classifier diagnostics (classifier_diagnostics.csv), fixed after the first section 3 run showed that the d = 8 filter did not change state through 2008 and that most label changes fell on refit dates, and before any factor return was loaded. The rule and its outcome are in decisions/primary_feature_set.md. The non-primary set is rerun in step 6.6." This supersedes the earlier wording that the question was decided before any classifier output existed.
   - PLAN.md: step 6.6 becomes "rerun with the non-primary feature set" (d = 8 if primary is d = 7, else d = 7), otherwise unchanged. Step 7.4 "What did not work" reports classifier_diagnostics.csv in full.

3. Review evidence, appended to review/section_3.md under a "Revision" heading: classifier_diagnostics.csv in full; anchor_chain.csv in full for the primary set; the filtered label and max probability for 2007-06-30 to 2009-12-31 and 2019-12-31 to 2021-06-30 for both sets; the refit dates on which a label change occurs for both sets; the per-refit state means pivoted wide for the primary set; the smoothed label runs (start, state, length) for both sets; the 5 largest matched_distance values with their refit and state. Update Tests run, Files changed and Reviewer reads.

4. pytest -q must pass. No factor returns are loaded or joined. Commit the revision as `section 3: chained anchoring and pre-registered primary feature set`, write instructions/03b_section_3_revision.status.md per rule 11, commit it, push, then stop.
