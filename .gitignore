# data/raw/ (pinned raw snapshots, zips and manifest.csv) is committed in full;
# interim, processed and external are always regenerated and never committed
data/interim/**
data/processed/**
data/external/**
!data/**/.gitkeep
.env
__pycache__/
.pytest_cache/
.venv/
*.egg-info/
